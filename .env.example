# PostgreSQL Passwort (sicher generieren!)
POSTGRES_PASSWORD=change_me_to_secure_password

# Secret Key für Anlagen-Hash-Generierung (sicher generieren!)
# Generieren mit: openssl rand -hex 32
SECRET_KEY=change_me_to_secure_random_string

# Erlaubte Origins für CORS (kommasepariert)
ALLOWED_ORIGINS=https://energie.raunet.eu
